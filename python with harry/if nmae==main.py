import mubeen2

# here ifwe can check the terminal so the line we can write one time it can execute multiple time and this is a problem so avoid this we can use some method in our mubeen2.py file so then it can execute one time

mubeen2.welcome()


# import os
